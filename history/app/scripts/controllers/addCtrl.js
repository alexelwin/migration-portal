'use strict';

/**
 * @ngdoc function
 * @name migrationApp.controller:addCtrl
 * @description
 * # addCtrl
 * Controller of the migrationApp
 */
angular.module('migrationApp').controller('addCtrl', ['$scope', function ($scope) {
    $scope.hello = 'hey. you can add a storage item here';
  }]);
